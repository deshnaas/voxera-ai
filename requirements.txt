# Voxera — core runtime deps (Python 3.12).
# Pinned to what is installed and verified on the demo machine (.venv312).

# Speech-to-text
openai-whisper==20250625

# Priya / Kokoro text-to-speech
kokoro==0.9.4
misaki==0.9.4
phonemizer==3.4.0
phonemizer-fork==3.3.2
espeakng-loader==0.2.4

# ML runtime
torch==2.13.0
numpy==2.5.2
scipy==1.18.1
numba==0.67.0

# Audio I/O + optional AEC barge-in
sounddevice==0.5.6
soundfile==0.14.0
noisereduce==3.0.3
pywebrtc-audio==0.2.0

# LLM (Ollama HTTP) + database + misc
requests==2.34.2
ollama==0.6.2
supabase==2.31.0
python-dotenv==1.2.3

# --- Patient Intelligence (voxera_patientfetch) ---------------------------
# Prescription OCR: PaddleOCR detection/recognition models on ONNX (no PaddlePaddle framework)
rapidocr-onnxruntime==1.4.4
pypdf==6.19.0
pypdfium2==5.13.0
# Patient-records API (dashboard talks to this; the service-role key never leaves the server)
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32
# Tests / SQL validation
pytest==9.1.1
pglast==8.4
# NOTE: SpeechBrain (voice-signal model) is NOT installed here. It needs torchaudio, which has no build
# for torch 2.13, so it lives in an isolated venv: see voxera_patientfetch/requirements_voice.txt

# --- Multilingual voice (voxera_multilang) --------------------------------
# int8 CTranslate2 Whisper, built locally from the small.pt already on disk:
#   python -m voxera_multilang.build_fast_stt
faster-whisper==1.2.1
ctranslate2==4.8.2
websockets==15.0.1
